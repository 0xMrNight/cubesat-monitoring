Machine Learning Models

The models folder contains the trained machine-learning artifacts used for CubeSat anomaly detection.

- Isolation Forest – Detects abnormal telemetry patterns.
- Scaler – Preprocesses telemetry features before ML inference.

These trained artifacts are loaded by the backend during anomaly detection.
